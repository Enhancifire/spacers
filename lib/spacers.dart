library spacers;

import 'package:flutter/widgets.dart';

addVerticalSpace(double height) {
  return SizedBox(
    height: height,
  );
}

addHorizontalSpace(double width) {
  return SizedBox(
    width: width,
  );
}
